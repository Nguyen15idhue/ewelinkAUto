<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <i class="fas fa-satellite-dish text-info me-2"></i>CGBAS MANAGER
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link <%= typeof pageTitle !== 'undefined' && pageTitle === 'dashboard' ? 'active' : '' %>" href="/">
                        <i class="fas fa-home me-1"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= typeof pageTitle !== 'undefined' && pageTitle === 'queue' ? 'active' : '' %>" href="/queue">
                        <i class="fas fa-tasks me-1"></i> Hàng đợi
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= typeof pageTitle !== 'undefined' && pageTitle === 'history' ? 'active' : '' %>" href="/history">
                        <i class="fas fa-history me-1"></i> Lịch sử
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user-circle me-1"></i> <%= user ? user.username : 'Admin' %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Đăng xuất</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>