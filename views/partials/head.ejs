<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CGBAS Manager</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body { background-color: #f4f6f9; font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; font-size: 0.9rem; }
        .navbar { box-shadow: 0 2px 4px rgba(0,0,0,.1); }
        .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,.075); margin-bottom: 1.5rem; }
        .card-header { background-color: #fff; border-bottom: 1px solid rgba(0,0,0,.05); font-weight: 600; }
        
        /* Status Badges */
        .badge-status { min-width: 80px; }
        .table > :not(caption) > * > * { vertical-align: middle; }
        
        /* Dashboard Specific */
        .stat-card { transition: transform 0.2s; }
        .stat-card:hover { transform: translateY(-3px); }
        .blinking { animation: blink 1.5s infinite; color: #d63384; font-weight: 600; }
        @keyframes blink { 50% { opacity: 0.5; } }

        /* Queue/History Colors */
        .status-PENDING { border-left: 4px solid #ffc107; background-color: #fff3cd33; }
        .status-PROCESSING { border-left: 4px solid #0dcaf0; background-color: #cff4fc33; }
        .status-COMPLETED { border-left: 4px solid #198754; }
        .status-FAILED { border-left: 4px solid #dc3545; background-color: #f8d7da33; }
    </style>
</head>